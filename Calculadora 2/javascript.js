function calcularidade() {
    
    var diaDigitado = document.getElementById('inputDia').value;
    var mesDigitado = document.getElementById('inputMes').value;
    var anoDigitado = document.getElementById('inputAno').value;
    var hoje = new Date() 
    var anohoje = new Date().getFullYear();
    var idade = anohoje - anoDigitado;
    var aniversario = new date (anohoje = "/" + mesDigitado + "/" + diaDigitado);
      document.getElementById('resultado').innerHTML = "Sua idade é: " + idade + " anos.";

      if (hoje > aniversario)
    
     idade --

     select
}